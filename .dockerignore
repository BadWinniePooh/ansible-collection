# Version control
.git
.gitignore

# Memory bank & docs — session context, not needed at runtime
memory-bank-branches/
docs/

# Dockerfiles — not needed at runtime
Dockerfile

# Ansible Vault secrets — must be mounted at runtime, never baked in
**/vault.yml

# Local environment config (direnv) — container uses ENV vars directly
.envrc

# Editor / IDE
.vscode/
*.swp
*.swo

# Python bytecode
__pycache__/
*.pyc

# macOS
.DS_Store
