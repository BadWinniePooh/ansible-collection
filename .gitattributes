# Default: normalise to LF in the repo, checkout as-is on each platform
* text=auto

# Shell scripts must always use LF — CRLF breaks them on Linux
*.sh text eol=lf

# Ansible / YAML files: LF
*.yml text eol=lf
*.yaml text eol=lf
*.j2  text eol=lf

# Dockerfile: LF
Dockerfile text eol=lf

# Windows-only files: CRLF if needed (none right now)
