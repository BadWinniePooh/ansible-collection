---
- name: Common role task
  ansible.builtin.debug:
    msg: "Running common role on {{ inventory_hostname }}"
  tags: [common, info]

- name: Preview rendered motd template (local render, no SSH needed)
  ansible.builtin.debug:
    msg: "{{ lookup('template', 'motd.j2') }}"
  tags: [common, template]

- name: Show db_password variable
  ansible.builtin.debug:
    msg: "DB password is: {{ db_password }}"
  tags: [vault]